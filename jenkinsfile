pipeline {
    agent any

    stages {
        stage('Test') {
            steps { 
                echo 'Testing...'
                sh 'npm test'
            }
        }
        stage('Test execution reporting') {
            steps {
                junit 'results/cypress-report.xml'
            }
        }
    }

}